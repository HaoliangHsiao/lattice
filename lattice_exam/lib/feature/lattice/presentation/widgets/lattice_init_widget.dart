import 'package:flutter/material.dart';

class LatticeInitWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lattice"),
      ),
      body: Container(),
    );
  }
}
